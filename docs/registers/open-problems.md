<!-- Generated by tools/problem_registers.py. Do not edit by hand.

The entry lives on the tracker; this file is a projection of it, so it cannot
disagree with the issue. Edit the field block on the issue or the comment and
regenerate. The checked-in file may be briefly stale and is never the
authority. -->

# The open problems register

**An open problem is a stateable failure the architecture is expected to have, deliberately unresolved, carrying a cutoff.** It is not a ticket: a ticket is work someone will do, a problem is a known gap nobody is working on, on purpose, because the bet is that the architecture grows past it.

A problem is admitted if it has a **statable failure** — a named way the architecture is expected to fall short, which someone could recognise happening. *The disagreement floor might be too high* is not admissible. **Only a grilling session mints one** ([ADR-0029](../adr/0029-a-problem-is-minted-by-a-human-a-proposal-is-not.md)); an agent that finds a problem files a `wayfinder:grilling` ticket.


**Before you implement anything, read all three registers**, and say in your PR
body what you found — `docs/agents/registers.md`, *Before you implement
anything*. "Nothing relevant" is a complete answer and the expected one; silence
is not, because an instruction nobody can tell was skipped decays.


## Comments this register could not read

A comment carrying a field block that does not parse is **skipped rather than
fatal** ([#354](https://github.com/NGL321/patchworks/issues/354)): one
unreadable comment used to abort the generator, and none of the three registers
regenerated until somebody edited it. But a skip nobody can see is the failure
the field-block mechanism exists to prevent, so each one is named here.

**A row in this section means the file below may be incomplete.** The comment
might have been a proposal, a rig report against a cutoff, or a dismissal —
and a dismissal *binds*, so a row here is a reason to open the comment before
trusting this page rather than a footnote. The fix is to edit the comment so
its block parses; never this file.

None. Every field block on every comment read.


## Uncut — nobody has said when this stops being tolerable

Admitted, and loud. These sort first and are stated as a debt, in the voice `@flexibility unknown` uses in the constants registers: *nobody has said when this stops being tolerable* is a fact, and hiding it is worse than showing it.

* [#326](https://github.com/NGL321/patchworks/issues/326) — *A private direction that survives reconciliation still has to survive being compressed into a `k`-dimensional chart by a frozen, shared `encode`, and nothing guarantees it does. `ker δ` is exactly invariant under reconciliation; the masks are construction-fixed and never re-open, so the alignment is bounded at build time. Signature: cells whose `‖Δ(private component)‖` trace is healthy in the stalk while prediction error shows no benefit from it — private structure held and never consumed.*
* [#330](https://github.com/NGL321/patchworks/issues/330) — *Core cells carry long-range agreement **or** slow state, not both, and which one gives way is decided by nothing. `dim H0 >= Σ_v max(0, n − Σ_e m_e)` sizes a core cell's private dimension at construction; ADR-0028 puts retention in the chart and `05` puts slow content in `ker δ`; the static wedge routes every long-range linguistic dependency into the same cells.*
* [#331](https://github.com/NGL321/patchworks/issues/331) — *A language run that fails is uninterpretable: nothing distinguishes an architecture that cannot do it from a domain in which nothing could. `03` bounds its world with a scripted controller solving 14 of 72 — dumb, privileged, and *lower*-bounding — so a sandbox failure can be attributed. Nothing in the language domain corresponds.*
* [#334](https://github.com/NGL321/patchworks/issues/334) — *One of the demo's two pass conditions cannot distinguish the thing it tests from a fixed property of the wiring. Non-overlapping latency IQRs between `perturb` and `retarget` follow from the two events entering at different hop distances, in a world of any depth and an architecture of any kind — a pure delay line would produce them. The residue: the demo has one load-bearing clause and one decorative one, the decorative one is the more legible on screen, and nothing on the surface tells a viewer which is which.*
* [#336](https://github.com/NGL321/patchworks/issues/336) — *Deep cells, which need the longest retention, become the most sensitive to their own updates. A prediction-rule step harmless at the rim moves an apex cell's retention constants a long way, and the retention gradient is unstable in exactly the cells that need it most. This is not gradient vanishing and not the settling floor; it is a third thing, and the design has no answer named for it.*
* [#340](https://github.com/NGL321/patchworks/issues/340) — *Someone reads a generalisation result off the `heldout_sector` split and attributes it to held-out target location, when part of the effect is a systematically emptier distractor field. Measured over 600 draws per split, the fraction of non-target pucks lying in the wedge is 0.104 on `train` against 0.034 on `heldout_sector`, unconditioned rate 0.098 — so it is `heldout_sector` that is displaced.*
* [#342](https://github.com/NGL321/patchworks/issues/342) — *The reconciliation gain divides by a term that grows with degree. A virtual node, relay hub or star wired to all 150 predicting cells therefore has a gain roughly 25x worse than an ordinary cell's, making it the least-reconciled cell in the graph the moment it is built; and it raises `deg(v)` at every cell it touches, degrading the same denominator term the hub was added to relieve. Self-defeating at both ends, and checkable at construction from `DomeSpec` with no run.*
* [#344](https://github.com/NGL321/patchworks/issues/344) — *The cheap spectral reading of retention is only licensed where a cell stays in one activation region long enough for its operator's own retention to express: dwell > tau (ADR-0005, re-pointed by #226). tau is a property of the cell's learned operator; dwell is set by how fast the graph's chatter walks the operating point across a crease. They are measurably independent — corr(log tau, log dwell) = -0.110 live, matching #42's construction-time -0.006 — and nothing in the architecture makes the licence hold. Recognisable as a spectral tau quoted at a cell whose dwell does not license it: read with both quantities off the same run on the corrected full-loop operator (#361), the median dwell/tau is 3.923 and 19 of 150 cells breach the licence at seed 42's own horizon, with the band running 2.165 to 10.984 across nine seeds and no seed failing at the median.*
* [#345](https://github.com/NGL321/patchworks/issues/345) — *A decision rests on a ground — a measurement or an argument — the ground is later retracted, and nothing walks back to the decisions that rested on it. Exclusions and declines therefore outlive their grounds. Recognisable as a spec or map section stating as current a premise a later ticket struck. A sub-shape: the ground never existed at all, was written into the source document by the decision that assumed it, and became self-confirming.*


## Cutoffs naming a firing condition nothing will reach

A cutoff that reads as watched and is not. This is `uncut` **wearing a disguise, and strictly worse than `uncut`**, because it does not read as a debt: the row says the problem is being watched, and nothing will ever fire. Both kinds of cutoff can wear it, in several states, separated because the fix differs — a `measurement` naming a rig that does not exist, or a real rig nobody has run; an `event` naming an issue the tracker does not have, an issue that had already closed when the problem was minted, or a live issue that has not moved since.

* [#341](https://github.com/NGL321/patchworks/issues/341) — `detectability` has never reported against this problem, so nothing has fired. *Any mechanism that computes a modulation centrally and distributes it outward must send it through the same per-hop taper it was invented to correct. Nothing broadcasts — ADR-0011 enforces locality — so the prior travels by edges as ordinary stalk content under the measured taper. At the standing 921x per hop over seven levels it arrives at ~1e-17 of its emitted magnitude, below every floor on the path, so the modulation cannot modulate anything.*
* [#439](https://github.com/NGL321/patchworks/issues/439) — `spectral_floor_read` has never reported against this problem, so nothing has fired. *At a cell whose every incident map is pinned, `λ_max(Σ_e F_evᵀF_ev)` is bounded by nothing and drifts to the fully-coherent case `g_v²·deg(v)`, so `reconciliation_gain` divides by a number that is not a bound there.*
* [#454](https://github.com/NGL321/patchworks/issues/454) — `#453` has not moved since this problem was minted, so nothing is carrying it toward closing. *The transport rule builds one composed channel faster than any per-map constraint spreads it. Composed rim-to-apex transport is rank 1 and stays rank 1 under ADR-0032's spectral floor: per hop the floor moves off-channel energy share 0.051 -> 0.381 at 100k, while the composed six-hop chain holds effective rank 1.000 and pays 7.3x of `sigma_max`. The trained surface composes ~240x below its own chance-alignment null (off-channel share 2.1e-4 against 0.051), so cross-edge alignment is driven away from the identity by the learning rule itself, and no local rule has been shown to move it back. Recognisable as a per-map remedy that pays end to end and delivers only per hop.*


A run is recorded by a `@rig` field block on a comment on the problem, which is what the rig report files ([#284](https://github.com/NGL321/patchworks/issues/284), `tools/cutoff_report.py`), or by `register:overdue`, since a bar cannot be crossed without the rig running. The report files **every evaluated run** and not only a crossing, so a rig that runs regularly and never crosses leaves this section rather than sitting in it. A `measurement` row here therefore means one of two things and not a third: the rig has genuinely not run, or it has not been given the hook.


An `event` row is **listed, not demoted**: the event may be exactly the right moment, and the listing exists to make the dormancy visible rather than to judge the cutoff. Dormancy is read off the named issue's own last activity against this problem's minting date — *has anything happened there since this problem started cutting on it* — which is two facts the tracker already holds and no chosen duration. It is the same question the `measurement` arm asks, which is why the two share a section: a run counts against **this problem**, not against the rig's whole history.


## Open problems

| problem | failure | cutoff | discovered | issue |
|---|---|---|---|---|
| One bias vector places the folds and preserves the private directions, and nothing guarantees the second | A private direction that survives reconciliation still has to survive being compressed into a `k`-dimensional chart by a frozen, shared `encode`, and nothing guarantees it does. `ker δ` is exactly invariant under reconciliation; the masks are construction-fixed and never re-open, so the alignment is bounded at build time. Signature: cells whose `‖Δ(private component)‖` trace is healthy in the stalk while prediction error shows no benefit from it — private structure held and never consumed. | `uncut` | #290, from #285's sweep of `docs/spec/05-timescales.md` and `docs/spec/06-graph-topology.md` (both *Known exposure*) | [#326](https://github.com/NGL321/patchworks/issues/326) |
| The wedge forces long-range agreement into the core, where slow state already lives | Core cells carry long-range agreement **or** slow state, not both, and which one gives way is decided by nothing. `dim H0 >= Σ_v max(0, n − Σ_e m_e)` sizes a core cell's private dimension at construction; ADR-0028 puts retention in the chart and `05` puts slow content in `ker δ`; the static wedge routes every long-range linguistic dependency into the same cells. | `uncut` | #296, from #285's sweep of `docs/spec/11-the-language-graph.md` (*Where the static wedge hurts*) | [#330](https://github.com/NGL321/patchworks/issues/330) |
| The language domain has no lower bound on its own solvability | A language run that fails is uninterpretable: nothing distinguishes an architecture that cannot do it from a domain in which nothing could. `03` bounds its world with a scripted controller solving 14 of 72 — dumb, privileged, and *lower*-bounding — so a sandbox failure can be attributed. Nothing in the language domain corresponds. | `uncut` | #297, from #285's sweep of `docs/spec/12-the-interlocutor.md` (*Known exposure*) | [#331](https://github.com/NGL321/patchworks/issues/331) |
| The demo's latency ordering restates hop count, and no guard on this graph separates it from hierarchy | One of the demo's two pass conditions cannot distinguish the thing it tests from a fixed property of the wiring. Non-overlapping latency IQRs between `perturb` and `retarget` follow from the two events entering at different hop distances, in a world of any depth and an architecture of any kind — a pure delay line would produce them. The residue: the demo has one load-bearing clause and one decorative one, the decorative one is the more legible on screen, and nothing on the surface tells a viewer which is which. | `uncut` | #301, from #285's sweep of ADR-0027 (*Consequences*) and `docs/spec/08-the-acceptance-demo.md` | [#334](https://github.com/NGL321/patchworks/issues/334) |
| Long retention buys parameter sensitivity, and a dense K cannot use the field's mitigations | Deep cells, which need the longest retention, become the most sensitive to their own updates. A prediction-rule step harmless at the rim moves an apex cell's retention constants a long way, and the retention gradient is unstable in exactly the cells that need it most. This is not gradient vanishing and not the settling floor; it is a third thing, and the design has no answer named for it. | `uncut` | #303, from #285's sweep of ADR-0028 (*Consequences*), citing #167 | [#336](https://github.com/NGL321/patchworks/issues/336) |
| The sector split's confound goes live the moment anything reads that axis | Someone reads a generalisation result off the `heldout_sector` split and attributes it to held-out target location, when part of the effect is a systematically emptier distractor field. Measured over 600 draws per split, the fraction of non-target pucks lying in the wedge is 0.104 on `train` against 0.034 on `heldout_sector`, unconditioned rate 0.098 — so it is `heldout_sector` that is displaced. | `uncut` | #309, from #285's sweep of `docs/spec/03-the-sandbox.md` (*Two limitations of the sampler, on the record*) | [#340](https://github.com/NGL321/patchworks/issues/340) |
| A hub incident on every predicting cell is the worst-reconciled cell in the graph, by construction | The reconciliation gain divides by a term that grows with degree. A virtual node, relay hub or star wired to all 150 predicting cells therefore has a gain roughly 25x worse than an ordinary cell's, making it the least-reconciled cell in the graph the moment it is built; and it raises `deg(v)` at every cell it touches, degrading the same denominator term the hub was added to relieve. Self-defeating at both ends, and checkable at construction from `DomeSpec` with no run. | `uncut` | #311, session bb1662a9-adc5-4442-a05b-21de7cdc3d39 turn 24, via #286's transcript pass | [#342](https://github.com/NGL321/patchworks/issues/342) |
| Nothing couples dwell to tau, and the coupling is a precondition the timescale mechanism never enforces | The cheap spectral reading of retention is only licensed where a cell stays in one activation region long enough for its operator's own retention to express: dwell > tau (ADR-0005, re-pointed by #226). tau is a property of the cell's learned operator; dwell is set by how fast the graph's chatter walks the operating point across a crease. They are measurably independent — corr(log tau, log dwell) = -0.110 live, matching #42's construction-time -0.006 — and nothing in the architecture makes the licence hold. Recognisable as a spectral tau quoted at a cell whose dwell does not license it: read with both quantities off the same run on the corrected full-loop operator (#361), the median dwell/tau is 3.923 and 19 of 150 cells breach the licence at seed 42's own horizon, with the band running 2.165 to 10.984 across nine seeds and no seed failing at the median. | `uncut` | #311, session 5a6c0d26-188f-4b2f-9552-010488a7e777 turns 6 and 8, via #286's transcript pass | [#344](https://github.com/NGL321/patchworks/issues/344) |
| A decision citing a ground that is later retracted is never re-read | A decision rests on a ground — a measurement or an argument — the ground is later retracted, and nothing walks back to the decisions that rested on it. Exclusions and declines therefore outlive their grounds. Recognisable as a spec or map section stating as current a premise a later ticket struck. A sub-shape: the ground never existed at all, was written into the source document by the decision that assumed it, and became self-confirming. | `uncut` | #287's grilling session, generalising candidates #304, #305 and #306 | [#345](https://github.com/NGL321/patchworks/issues/345) |
| Disagreement can drain under drive, taking the only instrument that would show it **(overdue)** | Under drive, per-edge Dirichlet energy falls while per-edge effective rank — the participation ratio `(Σσᵢ²)² / Σσᵢ⁴` — slides toward 1 across the fleet. Both halves of the local learning rule are computed from disagreement, so when it goes they stop training, and the instrument that would show it is the same quantity. | measurement `driven_settling` — draining_effective_rank < 2 | #288, from #285's sweep of `docs/spec/01-cell-and-sheaf.md` (*Known exposure*) and `docs/research/231-the-record-read-back.md` §4 | [#324](https://github.com/NGL321/patchworks/issues/324) |
| The gauge confines every prediction to one subspace, and nothing has checked the stalks reach it | The residual's common component along `colspan(D)` does not fall with learning, across edges that share nothing else. `colspan(D)` is fixed and shared by every cell, so if the stalk configurations the world and reconciliation produce sit systematically off it, every cell's prediction is wrong by a component no learning can reach. | measurement `detectability` — conduction ratio >= 1 | #289, from #285's sweep of `docs/spec/01-cell-and-sheaf.md` (*The cell body*, *Three lossy maps, and a fourth kind*) | [#325](https://github.com/NGL321/patchworks/issues/325) |
| Lateral reconciliation may contribute nothing measurable, and the dome's main justification goes with it | Rebuild without lateral edges and the acceptance demo is unaffected. Then the dome is a recurrent CNN with a per-position change of basis, and reconciliation *at a level* bought nothing — the one mechanism `06` says distinguishes it from the convolutional literature. | event #101 | #291, from #285's sweep of `docs/spec/06-graph-topology.md` (*Known exposure*) | [#327](https://github.com/NGL321/patchworks/issues/327) |
| 256 of 264 boundary cells are visual, and the demo may rest on the other eight | Per-event depth traces and onset are insensitive to the vision cone: the corrections that carry the demo originate at somatomotor levels for all three events, including the puck teleport, which `03` places at ~4 hops *through vision* precisely so the intermediate rung is a visual one. Then 97% of the boundary, the whole vision cone, and the taper arithmetic built on it are serving a modality the result does not rest on. | event #100 | #292, from #285's sweep of `docs/spec/06-graph-topology.md` (*Known exposure*) | [#328](https://github.com/NGL321/patchworks/issues/328) |
| A learned K may never settle under sign-flipping evidence, and that is 07's own pre-registered falsification | A mid-depth cell's `K` never settles: its evidence sign-flips tick to tick, the prediction rule chases it, and the operator's spectrum wanders instead of converging on a retention constant. It lands directly on stage 2's retention gradient, because a wandering `K` has no stable `λ` to read a `τ` off. | measurement `detectability` — conduction ratio >= 1 | #295, from #285's sweep of `docs/spec/07-local-learning-rule.md` and ADR-0008 | [#329](https://github.com/NGL321/patchworks/issues/329) |
| If cross-piece coupling is nonlinear, the only element that could model it is the one nobody trains | Persistent, structured, irreducible disagreement on edges that survives ADR-0004's three-cause exclusion procedure — `colspan(D)` first, then curvature, self-intersection and the lag floor. Cross-piece coupling in the sandbox is nonlinear, and after the conversion the entire cell → edge → cell loop is linear with exactly one exception, `encode`, which is frozen. | measurement `edge_attribution` — residue_over_topology >= 1 | #300, from #285's sweep of ADR-0004 (*Consequences*) and ADR-0017 | [#333](https://github.com/NGL321/patchworks/issues/333) |
| The band's projection can only shorten retention, never lengthen it **(overdue)** | Deep cells' `τ` is driven down by an enforcement mechanism that has no counterpart pushing up. ADR-0015 restores the band by rescaling the whole operator, moving every eigenvalue by the same factor, so each firing shortens all of that cell's retention constants together. Retention is now `λ(K)` and long retention at the apex is what the conduction ratio needs (`\|loop\|` = 14 there), so the retention gradient stage 2 exists to produce is fought by construction. | measurement `projection_firing` — band_fights_retention >= 1 | #302, from #285's sweep of ADR-0028 (*Consequences*) | [#335](https://github.com/NGL321/patchworks/issues/335) |
| An option sits out of scope on arithmetic the map retracted, and nothing re-reads Out of scope | An option is permanently excluded by arithmetic that no longer exists and nobody notices, because `Out of scope` is the one list nobody re-reads. A ground is a number or an argument; numbers get retracted, and nothing in the record walks back through what was closed on them. | event #237 | #305, from #285's sweep of `docs/research/231-the-record-read-back.md` §3.1 | [#337](https://github.com/NGL321/patchworks/issues/337) |
| The transport rule has no fixed point at agreement, so restriction maps wander at ~eta forever | Restriction maps never settle. At agreement the transport rule's gradient does not vanish — a norm, unlike a squared norm, has a gradient of unit magnitude arbitrarily close to zero — so every edge's maps orbit their optimum at amplitude `~η`, permanently. This is a floor on disagreement that is neither of ADR-0007's two kinds: not static (not a function of configuration) and not lag (not a function of motion), but a function of the rule itself, and it is the floor the acceptance demo reads through. | measurement `floor_split` — wander_over_named_floors >= 1 | #308, from #285's sweep of `docs/research/231-the-record-read-back.md` §4, quoting #89 | [#339](https://github.com/NGL321/patchworks/issues/339) |
| A prior computed at the apex cannot reach the rim it exists to modulate | Any mechanism that computes a modulation centrally and distributes it outward must send it through the same per-hop taper it was invented to correct. Nothing broadcasts — ADR-0011 enforces locality — so the prior travels by edges as ordinary stalk content under the measured taper. At the standing 921x per hop over seven levels it arrives at ~1e-17 of its emitted magnitude, below every floor on the path, so the modulation cannot modulate anything. | measurement `detectability` — outbound conduction ratio >= 1 | #311, session bb1662a9-adc5-4442-a05b-21de7cdc3d39 turn 24, via #286's transcript pass | [#341](https://github.com/NGL321/patchworks/issues/341) |
| The wedge's rim supplies the language domain's memory as a store, and the map's position is that memory is sustained | The wedge supplies the language domain's short-horizon memory as a 128-slot external store: 255 of the rim's 256 boundary cells hold a shift of a record and 2 hold new world content, so nothing distinguishes a graph that has learned to hold linguistic structure from one handed a record of it. If #375 reports no sustained structure, the rim buffer is the only memory the wedge has, and the map's adopted position — persistence is sustained, not stored — has no referent in this domain. | event #375 | #169, the grilling session that ruled the spoken rim's contract onto the slot | [#388](https://github.com/NGL321/patchworks/issues/388) |
| The incoherence cap is assumed but never enforced where a cell's incidence is entirely pinned | At a cell whose every incident map is pinned, `λ_max(Σ_e F_evᵀF_ev)` is bounded by nothing and drifts to the fully-coherent case `g_v²·deg(v)`, so `reconciliation_gain` divides by a number that is not a bound there. | measurement `spectral_floor_read` — gram_peak_over_cap_max > 1 | #435, reading ADR-0032's second pre-registration at 100k | [#439](https://github.com/NGL321/patchworks/issues/439) |
| The transport rule builds one composed channel faster than any per-map constraint spreads it | The transport rule builds one composed channel faster than any per-map constraint spreads it. Composed rim-to-apex transport is rank 1 and stays rank 1 under ADR-0032's spectral floor: per hop the floor moves off-channel energy share 0.051 -> 0.381 at 100k, while the composed six-hop chain holds effective rank 1.000 and pays 7.3x of `sigma_max`. The trained surface composes ~240x below its own chance-alignment null (off-channel share 2.1e-4 against 0.051), so cross-edge alignment is driven away from the identity by the learning rule itself, and no local rule has been shown to move it back. Recognisable as a per-map remedy that pays end to end and delivers only per hop. | event #453 | #437's grilling session, cutting from #436 sections 3 and 4 | [#454](https://github.com/NGL321/patchworks/issues/454) |


## Resolved

**A closed problem stays in the register.** The set of problems that dissolved on their own is the direct evidence for the bet this project is making — that constraint left out is supplied by the architecture rather than specified — and deleting the row deletes the evidence.

| problem | failure | cutoff | discovered | issue |
|---|---|---|---|---|
| The composed dome may be globally contracting, and ADR-0015 deferred the bound to work that has since reported | Every cell's `σ_max(K)` is bounded at 1 and every interior map's `‖F‖_F` at `ρ = 2`, both enforced per-cell by projection. Nothing bounds the composition. Contracting: composed loop gain below 1 everywhere, activity decays to the quiescent hold, and the graph sustains nothing between world writes. Expanding: some cycle composes above 1 and the dome sustains activity that came from nowhere in the world. | event #237 | #299, from #285's sweep of ADR-0015 (*The composed system: named here, argued elsewhere*) | [#332](https://github.com/NGL321/patchworks/issues/332) |
| 06 declines spectrum control on a budget the conversion was taken to free | A live over-squashing mitigation for the exact failure stage 2 exists to fix sits declined in the spec, on a premise the project itself dismantled, in a document that still states the premise as current. `06` declines Arroyo et al.'s direct control of the Jacobian spectrum because the spectrum is spent three ways; two of the three claimants have since changed. | event #237 | #306, from #285's sweep of `docs/research/231-the-record-read-back.md` §3.2 | [#338](https://github.com/NGL321/patchworks/issues/338) |
| ADR-0016's written-or-read split may put proprioceptive feedback one hop from the action that caused it | A motor panel that also receives proprioceptive feedback on the action just taken is a single interface in the world. Under ADR-0016's written-or-read ban that feedback must land on a different boundary cell, so the shortest loop from a command to its own consequence grows by at least two ticks — and `\|loop(c)\|` is exactly the divisor of ADR-0026's conduction ratio. The split therefore raises the bar the architecture must clear, for a reason about tick bookkeeping rather than about the world. | measurement `loop_length` — loop_split_cost >= 2 | #311, sessions a3887498-5d2c-42c7-a561-9298e39a4d27 turn 23 and 7bc7c089-ad9d-49c0-97af-fb1d9b99c4d0 turn 5, via #286's transcript pass | [#343](https://github.com/NGL321/patchworks/issues/343) |
| ADR-0026's divisor is the graph's round trip, not the loop through the world it argues for **(overdue)** | ADR-0026's conduction ratio divides by `\|loop(c)\| = 2 · d(c, rim)`, a round trip that turns around at the rim **as a set** and never leaves the graph. The loop the predicate is justified by — *the cell still holds what it sent by the time the answer gets back* — leaves through an actuator, crosses the world, and re-enters at a **different** boundary cell under ADR-0016's ban. Where the two lengths differ the bar is read against the shorter one, passing cells whose real round trip their retention cannot hold. | measurement `loop_length` — world_loop_excess >= 1 | #362, off #351's reading on #343 | [#368](https://github.com/NGL321/patchworks/issues/368) |
| Private capacity and transmitted capacity are one budget, and lambda is the only dial on it | Private capacity and transmitted capacity are one budget -- `dim H^0 = dim C^0 - rank δ` -- and the sparsity pressure `lambda` is the only dial on it. `05-timescales.md` turns it toward private state and ADR-0022's channel reads the other end, and no document prices them together. The architecture can therefore fail both halves of its own bar at once: `tau-hat_c` is a decay time read on private features, and the deviation being read has to arrive through the channel. Signature: effective rank and the conduction ratio moving in opposite directions across a sweep of `lambda`, with no setting where both clear. | event #393 | #356 | [#395](https://github.com/NGL321/patchworks/issues/395) |


## Stated gaps

One thing this projection cannot reach, named rather than silently omitted.

* **The ground a closed problem closed on.** `docs/agents/registers.md` puts the
  ground — dissolved, solved or withdrawn — in a *closing comment*, as prose,
  with no field block of its own. A projection may not restate prose, so the
  resolved row links to the issue and the ground is one click away rather than
  copied here. Giving the ground a field would change that, and is the design's
  call rather than the generator's.
